const express = require('express'); const controller = require('../controllers/productController'); const asyncHandler = require('../utils/asyncHandler'); const { validateBody, validateId } = require('../middlewares/validate'); const router = express.Router();
router.get('/', asyncHandler(controller.getProducts)); router.post('/', validateBody('createProduct'), asyncHandler(controller.createProduct)); router.put('/:id', validateId, validateBody('updateProduct'), asyncHandler(controller.updateProduct)); router.delete('/:id', validateId, asyncHandler(controller.deleteProduct)); module.exports = router;

const express = require('express'); const controller = require('../controllers/categoryController'); const asyncHandler = require('../utils/asyncHandler'); const { validateBody } = require('../middlewares/validate'); const router = express.Router(); router.get('/', asyncHandler(controller.getCategories)); router.post('/', validateBody('createCategory'), asyncHandler(controller.createCategory)); module.exports = router;

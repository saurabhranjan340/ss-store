# SS Store deployment kit

Contains the production-hardened Express + Prisma backend, a frontend environment template, platform configs, Prisma migration, monitoring hooks, and a post-deploy smoke test.

The deployment itself is intentionally not claimed as live: provider credentials, GitHub access, and a real Next.js repository are required to publish.
